# 1. Length of objects: len(object) -> int

# 2. False | True

# 3. Counters: Creates key value map whre the key is the object and the value is the number of occurences
# from collections import Counter
# str s
# sCount = Counter(s) 
# Compare
# if sCount == tCount

# 4. Key Value Maps: 
# myKeyValueMap = {}
# myKeyValueMap[potentialKey] | myKeyValueMap.get(potentialKey, defaulValue)
# myKeyValueMap.update({key:value})
# myKeyValueMap.keys()
# myKeyValueMap.values()
# myKeyVauleMap.pop(ele) # To remove an element

# 5. Sets
# numSet = set(nums) # nums is a list of nums
# numSet.add(ele)
# numSet.remove(ele)
# numset.pop() # Removes the last ele in set
# if num in numSet:    O(1) runtime same as .get
# 

# 6. List Operations: 
#    Append to a list with .append().
#    Use .pop() to remove the last element.
#    .sort() to sort the list in place, and .reverse() to reverse it.
#    enumerate(nums)

# 7. Sorting: 
#    Use sorted() for a new sorted list, or .sort() to sort in place.

# 8. char validation
# ord(char) -> int
# chr(int) -> char